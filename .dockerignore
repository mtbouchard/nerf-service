__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
# NOTE: the only image we build is the GPU worker (worker/Dockerfile), and RunPod's
# GitHub builder uses the repo root as the build context — so do NOT ignore worker/.
.env
data/
tests/
client/
.git/
.DS_Store

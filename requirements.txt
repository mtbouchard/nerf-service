fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20
pydantic-settings>=2.6
pillow>=10.4
httpx>=0.28
# Only used when NERF_BACKEND=runpod (S3/R2 presigned URLs). Harmless to install otherwise.
boto3>=1.34
